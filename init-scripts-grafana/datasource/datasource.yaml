apiVersion: 1
datasources:
  - name: Infinity
    type: yesoreyeram-infinity-datasource
    access: proxy
    isDefault: true  # Configurar como data source predeterminado (opcional)
    jsonData:
      auth_method: none  # Configuración específica para Infinity
